<% content_for :title, "Reasignar partida" %>

<div class="max-w-4xl mx-auto px-4 py-6">
  <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-6 space-y-6">
    <div class="flex items-start justify-between gap-4">
      <div>
        <h1 class="text-2xl font-bold text-gray-900">Reasignar partida</h1>
        <p class="text-sm text-gray-600 mt-1">Mover los derechos de esta partida a otro agente aduanal. Se crearán servicios para el nuevo agente usando el mismo catálogo y se ocultará la partida original al agente saliente.</p>
      </div>
      <div class="text-right text-sm text-gray-500">
        <div><span class="font-semibold">BL:</span> <%= @bl_house_line.blhouse %></div>
        <div><span class="font-semibold">Partida:</span> <%= @bl_house_line.partida %></div>
      </div>
    </div>

    <%= form_with url: perform_reassign_bl_house_line_path(@bl_house_line), scope: :reassign, method: :patch, class: "space-y-5" do |f| %>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <%= f.label :new_customs_agent_id, "Nuevo agente aduanal", class: "block text-sm font-semibold text-gray-800 mb-1" %>
          <%= f.select :new_customs_agent_id,
            options_from_collection_for_select(@customs_agents, :id, :name, params.dig(:reassign, :new_customs_agent_id) || @customs_agents.first&.id),
            { include_blank: "Selecciona un agente" },
            class: "w-full rounded-lg border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500",
            data: {
              controller: "reassign-patents",
              "reassign-patents-url-value": reassign_patents_bl_house_line_path(@bl_house_line),
              "reassign-patents-frame-value": "patent_select",
              action: "change->reassign-patents#changeAgent"
            } %>
        </div>
        <div>
          <%= f.label :new_customs_agent_patent_id, "Patente", class: "block text-sm font-semibold text-gray-800 mb-1" %>
          <%= turbo_frame_tag "patent_select" do %>
            <%= render partial: "bl_house_lines/patent_select", locals: { patents: @customs_agent_patents, selected_patent_id: params.dig(:reassign, :new_customs_agent_patent_id) } %>
          <% end %>
        </div>
        <div>
          <%= f.label :new_client_id, "Cliente a facturar", class: "block text-sm font-semibold text-gray-800 mb-1" %>
          <%= turbo_frame_tag "client_select" do %>
            <%= render partial: "bl_house_lines/client_select", locals: { clients: @clients, selected_client_id: params.dig(:reassign, :new_client_id) } %>
          <% end %>
        </div>
        <div class="flex items-center mt-6">
          <%= f.check_box :hide_original, { checked: params.dig(:reassign, :hide_original).nil? ? true : ActiveModel::Type::Boolean.new.cast(params.dig(:reassign, :hide_original)), class: "h-4 w-4 text-indigo-600 border-gray-300 rounded" }, "true", "false" %>
          <%= f.label :hide_original, "Ocultar partida original al agente saliente", class: "ml-2 text-sm text-gray-700" %>
        </div>
      </div>

      <div class="rounded-xl bg-amber-50 border border-amber-200 p-4 text-amber-800 text-sm space-y-1">
        <div class="font-semibold">Se hará lo siguiente:</div>
        <ul class="list-disc pl-5 space-y-1">
          <li>Renombrar la partida original agregando sufijo "R" para liberar el BL.</li>
          <li>Crear una nueva partida con el BL original y documentos ya validados.</li>
          <li>Copiar servicios al nuevo agente usando el mismo catálogo; los servicios originales permanecen.</li>
          <li>Notificar al nuevo agente.</li>
        </ul>
      </div>

      <div class="flex justify-end gap-2">
        <%= link_to "Cancelar", bl_house_line_path(@bl_house_line), class: "px-4 py-2 rounded-lg border border-gray-300 text-gray-700 hover:bg-gray-50" %>
        <%= f.submit "Reasignar", class: "px-5 py-2 rounded-lg bg-indigo-600 text-white font-semibold hover:bg-indigo-700 shadow-sm" %>
      </div>
    <% end %>
  </div>
</div>
