<%# Handle validation errors for new patent creation %>
<%= turbo_stream.replace "flash_messages" do %>
  <%= render partial: "shared/flash_messages", locals: { flash: flash } %>
<% end %>

<%= turbo_stream.replace "new-patent-modal" do %>
  <%= render partial: "customs_agent_patents/form", locals: { patent: @patent, entity: @entity } %>
<% end %>