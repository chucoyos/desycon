<%= turbo_frame_tag dom_id(patent) do %>
  <div class="patent-item flex items-center gap-2 p-3 bg-white border border-gray-200 rounded-lg mb-2">
    <div class="flex-1">
      <span class="text-sm font-medium text-gray-900"><%= patent.patent_number %></span>
    </div>
    <div class="flex items-center gap-2">
      <%= button_to entity_customs_agent_patent_path(patent.entity, patent),
          method: :delete,
          params: { context: "edit" },
          form: { data: { turbo_confirm: "¿Eliminar esta patente?" } },
          class: "p-1.5 text-red-600 hover:bg-red-50 rounded-lg transition-colors duration-150" do %>
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
        </svg>
      <% end %>
    </div>
  </div>
<% end %>
