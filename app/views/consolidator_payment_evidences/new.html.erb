<% content_for :title, "Adjuntar comprobante" %>

<div class="w-full px-4 sm:px-6 lg:px-8 py-4 sm:py-6 lg:py-8">
  <div class="max-w-3xl mx-auto">
    <div class="mb-6">
      <h1 class="text-2xl font-bold text-slate-900">Adjuntar comprobante de pago</h1>
      <p class="text-sm text-slate-500 mt-1">Seleccionaste <%= @selected_invoice_ids.size %> factura(s) para revisión.</p>
    </div>

    <div class="rounded-2xl border border-slate-200 bg-white shadow-sm p-5">
      <%= render "consolidator_payment_evidences/form" %>
    </div>
  </div>
</div>
