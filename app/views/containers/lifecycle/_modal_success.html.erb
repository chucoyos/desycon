<turbo-frame id="container_lifecycle_modal">
  <div class="fixed inset-0 z-50 overflow-y-auto" aria-labelledby="container-lifecycle-success-title" role="dialog" aria-modal="true">
    <div class="fixed inset-0 bg-slate-900/40 backdrop-blur-sm" onclick="document.getElementById('container_lifecycle_modal').innerHTML=''" aria-hidden="true"></div>

    <div class="flex min-h-full items-center justify-center p-4">
      <div class="relative w-full max-w-md overflow-hidden rounded-2xl bg-white shadow-xl ring-1 ring-black/5">
        <div class="flex items-center justify-between border-b border-slate-100 px-6 py-4">
          <h3 id="container-lifecycle-success-title" class="text-lg font-semibold text-slate-900">Actualización completada</h3>
          <button type="button" class="rounded-full p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-600" onclick="document.getElementById('container_lifecycle_modal').innerHTML=''">
            <span class="sr-only">Cerrar</span>
            <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        <div class="px-6 py-5">
          <p class="text-sm text-slate-700"><%= message %></p>
          <div class="mt-5 flex items-center justify-end gap-2">
            <button type="button" class="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50" onclick="document.getElementById('container_lifecycle_modal').innerHTML=''">Cerrar</button>
            <%= link_to "Recargar listado", containers_path, data: { turbo_frame: "_top" }, class: "rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-blue-700" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</turbo-frame>
